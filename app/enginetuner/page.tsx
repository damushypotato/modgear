import Navbar from '../ui/navbar';

// export the engine tuner page to display the engine tuner UI
export default function EngineTuner() {
    return (
        <main>
            <Navbar />
        </main>
    );
}
